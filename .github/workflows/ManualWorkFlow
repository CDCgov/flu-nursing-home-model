name: Manual Workflow
on:
  workflow_dispatch:  # This trigger allows manual execution

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2
      
      # Add more steps as needed
